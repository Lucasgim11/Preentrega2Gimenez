{
  "name": "lucasg",
  "version": "1.0.0",
  "description": "Intro a git y github. Que locura todo esto. Impresionante la cantidad de tiempo que necesitas para estar con esto. Unos verdaderos genios.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass",
    "sass:watch": "sass --watch scss/main.scss  css/main.css"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "sass": "^1.64.0"
  }
}
